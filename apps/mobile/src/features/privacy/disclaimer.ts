// Canonical privacy + framing copy (docs/02 principles 2 & 4). One source of truth so the
// "not medical / not detection / not monitoring" promise reads identically on every scan surface,
// each track's results, and the apparel tab. Surface-specific lines (e.g. hair's "not a follicle
// count") sit alongside these, never replacing them.

/** The non-negotiable framing line — shown on consent, both scan screens, results, and apparel. */
export const NOT_MEDICAL =
  'Not medical advice, not detection, not monitoring — just an encouraging trend.';

/** How the photo is handled — shown wherever a scan is taken. */
export const PHOTO_PROMISE =
  'Your photo is analyzed once and never stored. Everything stays on your device unless you turn on sync.';

/** Hair's honest caveat about a coarse, slow signal. */
export const HAIR_TREND = 'A coarse month-to-month trend — not a follicle count, not a diagnosis.';
